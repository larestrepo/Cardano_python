### Command instructions to setup python environment in Visual Studio

In powershell create folder for the environment and inside the folder type:
    
    code .
Create the new virtual environment:
   
    py -3 -m venv .venv

activate the environment

